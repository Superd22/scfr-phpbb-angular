import { FloatingMainHeaderDirective } from './floating-main-header.directive';

describe('FloatingMainHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingMainHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
