import { ImageHeaderFitDirective } from './image-header-fit.directive';

describe('ImageHeaderFitDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageHeaderFitDirective();
    expect(directive).toBeTruthy();
  });
});
