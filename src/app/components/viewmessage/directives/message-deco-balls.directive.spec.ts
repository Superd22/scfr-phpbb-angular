import { MessageDecoBallsDirective } from './message-deco-balls.directive';

describe('MessageDecoBallsDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageDecoBallsDirective();
    expect(directive).toBeTruthy();
  });
});
