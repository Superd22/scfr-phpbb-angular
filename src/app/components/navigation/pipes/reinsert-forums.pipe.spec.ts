import { ReinsertForumsPipe } from './reinsert-forums.pipe';

describe('ReinsertForumsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReinsertForumsPipe();
    expect(pipe).toBeTruthy();
  });
});
