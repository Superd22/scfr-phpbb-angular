import { LookForForumPipe } from './look-for-forum.pipe';

describe('LookForForumPipe', () => {
  it('create an instance', () => {
    const pipe = new LookForForumPipe();
    expect(pipe).toBeTruthy();
  });
});
