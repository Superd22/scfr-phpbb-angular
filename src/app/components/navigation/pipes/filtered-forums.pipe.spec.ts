import { FilteredForumsPipe } from './filtered-forums.pipe';

describe('FilteredForumsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredForumsPipe();
    expect(pipe).toBeTruthy();
  });
});
