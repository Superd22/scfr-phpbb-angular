import { ListStopAtBottomDirective } from './list-stop-at-bottom.directive';

describe('ListStopAtBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ListStopAtBottomDirective();
    expect(directive).toBeTruthy();
  });
});
