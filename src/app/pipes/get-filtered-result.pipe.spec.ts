import { GetFilteredResultPipe } from './get-filtered-result.pipe';

describe('GetFilteredResultPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFilteredResultPipe();
    expect(pipe).toBeTruthy();
  });
});
